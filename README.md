# assignment-cardiff
Personal Assignment
Run the following command in the command prompt in windows os or terminator in Linux os.

•	Create a new folder called_clean - mkdir eco_clean
•	Go to the folder - cd eco_clean
•	Create main file - touch main.cp
•	Open main file - nano main.cpp
•	Paste the C++ code into main.cpp
•	save the nano

Compile the program
•	sudo apt install g++
•	g++ -std=c++17 -O2 -Wall main.cpp -o eco_clean

Run it
•	./eco_clean
